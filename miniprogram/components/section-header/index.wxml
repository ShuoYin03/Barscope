<view class="section-hd">
  <view class="section-hd-left">
    <text class="section-num">{{num}}</text>
    <text class="section-title">{{title}}</text>
  </view>
  <view wx:if="{{more}}" class="section-more" bindtap="onMore">
    <text>查看全部 →</text>
  </view>
</view>
