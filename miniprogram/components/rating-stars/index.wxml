<view class="score-picker">
  <text
    wx:for="{{scores}}"
    wx:key="*this"
    class="score-btn {{item === value ? 'selected' : ''}} {{readonly ? 'readonly' : ''}}"
    bindtap="onTap"
    data-v="{{item}}"
  >{{item}}</text>
</view>
