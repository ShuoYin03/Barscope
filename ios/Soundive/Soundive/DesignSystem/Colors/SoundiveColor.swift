import SwiftUI

extension Color {

    static let soundiveBackground =
        Color(red: 0.04, green: 0.04, blue: 0.04)

    static let soundiveCard =
        Color(red: 0.10, green: 0.10, blue: 0.10)

    static let soundivePrimary =
        Color(red: 1.0, green: 0.35, blue: 0.15)

    static let soundiveText =
        Color.white

    static let soundiveSecondaryText =
        Color.gray
}
